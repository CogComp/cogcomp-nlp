(define features

    (define word-pos
        (list word pos))
    (define parse-sibling-features
        (list label
	        (head-features :parse-view *default-parser*
                    word-pos)))
  parse-sibling-features)
